'use strict';

angular.module('afredApp').controller('AdminProvincesController', [
  '$scope',
  'provinceResource',
  function($scope,
           provinceResource) {
    /* ---------------------------------------------------------------------
     * Functions.
     * --------------------------------------------------------------------- */
    
  }
]);
